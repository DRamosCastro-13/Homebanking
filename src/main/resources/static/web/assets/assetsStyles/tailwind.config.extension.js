/** @type {import('tailwindcss').Config} */
export const theme = {
    extend: {
        fontFamily: {
            'orbitron' : ['Orbitron', 'sans-serif']
        },
        colors: {
            
        }
    }
};
export const plugins = [];